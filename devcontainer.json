{
    "name": "Flask Server",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "forwardPorts": [5000],
    "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
    }
}
